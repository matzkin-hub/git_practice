## 「彩色錬成！色パレットで大輪を染めよ」

## ミッション：爆発するたびに、違う色の花火が出るようにしよう！

この課題では「配列からランダムに色を選ぶ」「色パレットを花火ごとに切り替える」技術を習得します。

花火は動きだけでなく、色の豊かさでも印象が大きく変わります。

### コードを写そう

HTMLタブに下のコードを貼り付けてください。

```html
<p class="hint">毎回違う色の花火を打ち上げよう</p>
<canvas id="canvas"></canvas>
```

CSSタブに下のコードを貼り付けてください。

```css
body {
  margin: 0;
  overflow: hidden;
  background: #000;
  font-family: sans-serif;
}

.hint {
  position: absolute;
  top: 16px;
  left: 16px;
  margin: 0;
  color: white;
  z-index: 1;
}

canvas {
  display: block;
}
```

JSタブに下のコードを貼り付けてください。

```js
const canvas = document.querySelector('#canvas');
const ctx = canvas.getContext('2d');

const rockets = [];
const particles = [];

const colorPalettes = [
  ['red', 'tomato', 'salmon', 'mistyrose'],
  ['lime', 'limegreen', 'lightgreen', 'honeydew'],
  ['blue', 'royalblue', 'lightskyblue', 'lavender'],
  ['yellow', 'gold', 'khaki', 'lightyellow'],
  ['magenta', 'orchid', 'violet', 'plum'],
  ['cyan', 'deepskyblue', 'paleturquoise', 'lightcyan']
];

function resizeCanvas() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
}

function drawSky() {
  ctx.fillStyle = '#06142b';
  ctx.fillRect(0, 0, canvas.width, canvas.height);
}

function explode(x, y, palette) {
  for (let i = 0; i < 60; i++) {
    const angle = Math.random() * Math.PI * 2;
    const speed = Math.random() * 5 + 2;
    const color = palette[Math.floor(Math.random() * palette.length)];

    particles.push({
      x: x,
      y: y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      gravity: 0.05,
      friction: 0.98,
      alpha: 1,
      size: Math.random() * 2 + 1,
      color: color
    });
  }
}

resizeCanvas();

window.addEventListener('resize', function() {
  resizeCanvas();
});

canvas.addEventListener('click', function(event) {
  const rect = canvas.getBoundingClientRect();
  const x = event.clientX - rect.left;
  const targetY = event.clientY - rect.top;
  const palette = colorPalettes[Math.floor(Math.random() * colorPalettes.length)];

  rockets.push({
    x: x,
    y: canvas.height,
    targetY: targetY,
    vy: -8,
    palette: palette
  });
});

function animate() {
  drawSky();

  for (let i = rockets.length - 1; i >= 0; i--) {
    const rocket = rockets[i];
    rocket.y = rocket.y + rocket.vy;

    ctx.beginPath();
    ctx.arc(rocket.x, rocket.y, 2, 0, Math.PI * 2);
    ctx.fillStyle = 'white';
    ctx.fill();

    if (rocket.y <= rocket.targetY) {
      explode(rocket.x, rocket.y, rocket.palette);
      rockets.splice(i, 1);
    }
  }

  for (let i = particles.length - 1; i >= 0; i--) {
    const particle = particles[i];

    particle.vy = particle.vy + particle.gravity;
    particle.vx = particle.vx * particle.friction;
    particle.vy = particle.vy * particle.friction;
    particle.x = particle.x + particle.vx;
    particle.y = particle.y + particle.vy;
    particle.alpha = particle.alpha - 0.015;

    ctx.beginPath();
    ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);
    ctx.fillStyle = particle.color;
    ctx.globalAlpha = particle.alpha;
    ctx.fill();
    ctx.globalAlpha = 1;

    if (particle.alpha <= 0) {
      particles.splice(i, 1);
    }
  }

  requestAnimationFrame(animate);
}

animate();
```

クリックするたびに、違う色の花火が出ることを確認してください。

### コードを読み解こう

#### 色パレットの配列

```js
const colorPalettes = [
  ['red', 'tomato', 'salmon', 'mistyrose'],
  ['lime', 'limegreen', 'lightgreen', 'honeydew']
];
```

1つの花火に使う色の組を、さらに配列にまとめています。つまり「配列の中に配列」が入っています。

#### 花火ごとにパレットを1つ選ぶ

```js
const palette = colorPalettes[Math.floor(Math.random() * colorPalettes.length)];
```

クリックされた瞬間に、その花火専用の色パレットを1つ決めています。

#### 粒ごとにパレットの中から色を選ぶ

```js
const color = palette[Math.floor(Math.random() * palette.length)];
```

同じ花火の中でも少し色が違うと、立体感が出ます。

#### サイズも少しずつ変える

```js
size: Math.random() * 2 + 1
```

全部同じ大きさより、少しばらつく方が自然に見えます。

### 改造しよう

#### ミッション①：新しい色パレットを追加しよう

オレンジや紫など、自分の好きな組み合わせを追加してみましょう。

#### ミッション②：粒の大きさの幅を変えよう

```js
Math.random() * 2 + 1
```

数字を変えると、ばらつき具合が変わります。

#### ミッション③：ロケット本体の色も変えよう

白以外にすると、打ち上げ中の印象も変わります。


#### チェックしてみよう

✅️ 配列からランダムに色パレットを選んでいる
✅️ 粒ごとに色を変えている
✅️ 花火ごとに色の雰囲気が変わる
✅️ 粒のサイズにばらつきがある

