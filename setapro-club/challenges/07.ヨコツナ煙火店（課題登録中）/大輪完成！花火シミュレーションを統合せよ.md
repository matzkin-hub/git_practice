## 「大輪完成！花火シミュレーションを統合せよ」

## ミッション：クリック打ち上げと自動打ち上げを備えた、花火シミュレーションを完成させよう！

この課題では、ここまで学んだ `Canvas`、アニメーション、配列、クラス、ランダム、DOMイベントをまとめて使います。

この10本の最終回です。ここまで来れば、`hanabi/main.js` で使っている技術をひと通り自分で扱える段階に入ります。

### コードを写そう

HTMLタブに下のコードを貼り付けてください。

```html
<div class="controls">
  <h2>🎆 花火シミュレーション</h2>
  <p>クリック: 花火を打ち上げ</p>
  <p>自動打ち上げ: <span id="autoStatus">OFF</span></p>
  <button id="toggleAuto">自動打ち上げ ON/OFF</button>
</div>
<canvas id="canvas"></canvas>
```

CSSタブに下のコードを貼り付けてください。

```css
body {
  margin: 0;
  padding: 0;
  background-color: #000;
  overflow: hidden;
  font-family: sans-serif;
}

canvas {
  display: block;
  background: linear-gradient(to bottom, #000428, #004e92);
}

.controls {
  position: absolute;
  top: 20px;
  left: 20px;
  color: white;
  background-color: rgba(0, 0, 0, 0.7);
  padding: 15px;
  border-radius: 8px;
  z-index: 10;
}

.controls h2 {
  margin: 0 0 10px 0;
  font-size: 18px;
}

.controls p {
  margin: 5px 0;
  font-size: 14px;
}

button {
  margin-top: 10px;
  padding: 8px 15px;
  background-color: #ff6b6b;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-size: 14px;
}

button:hover {
  background-color: #ff5252;
}
```

JSタブに下のコードを貼り付けてください。

```js
const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

window.addEventListener('resize', function() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
});

const colorPalettes = [
  ['red', 'tomato', 'salmon', 'mistyrose'],
  ['lime', 'limegreen', 'lightgreen', 'honeydew'],
  ['blue', 'royalblue', 'lightskyblue', 'lavender'],
  ['yellow', 'gold', 'khaki', 'lightyellow'],
  ['magenta', 'orchid', 'violet', 'plum'],
  ['cyan', 'deepskyblue', 'paleturquoise', 'lightcyan'],
  ['orange', 'darkorange', 'goldenrod', 'moccasin']
];

let rockets = [];
let particles = [];
let autoLaunch = false;

class Particle {
  constructor(x, y, color) {
    this.x = x;
    this.y = y;
    this.color = color;

    const angle = Math.random() * Math.PI * 2;
    const speed = Math.random() * 5 + 2;
    this.vx = Math.cos(angle) * speed;
    this.vy = Math.sin(angle) * speed;

    this.gravity = 0.05;
    this.friction = 0.98;
    this.alpha = 1;
    this.size = Math.random() * 2 + 1;
    this.trail = [];
    this.trailLength = 5;
  }

  update() {
    this.trail.push({ x: this.x, y: this.y });
    if (this.trail.length > this.trailLength) {
      this.trail.shift();
    }

    this.vy = this.vy + this.gravity;
    this.vx = this.vx * this.friction;
    this.vy = this.vy * this.friction;
    this.x = this.x + this.vx;
    this.y = this.y + this.vy;
    this.alpha = this.alpha - 0.01;
  }

  draw() {
    if (this.trail.length > 1) {
      for (let i = 0; i < this.trail.length - 1; i++) {
        const point = this.trail[i];
        const alpha = (i / this.trail.length) * this.alpha;

        ctx.beginPath();
        ctx.arc(point.x, point.y, this.size * 0.5, 0, Math.PI * 2);
        ctx.fillStyle = this.color;
        ctx.globalAlpha = alpha * 0.5;
        ctx.fill();
      }
    }

    ctx.beginPath();
    ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
    ctx.fillStyle = this.color;
    ctx.globalAlpha = this.alpha;
    ctx.fill();

    const gradient = ctx.createRadialGradient(this.x, this.y, 0, this.x, this.y, this.size * 3);
    gradient.addColorStop(0, this.color);
    gradient.addColorStop(1, 'transparent');
    ctx.beginPath();
    ctx.arc(this.x, this.y, this.size * 3, 0, Math.PI * 2);
    ctx.fillStyle = gradient;
    ctx.globalAlpha = this.alpha * 0.3;
    ctx.fill();

    ctx.globalAlpha = 1;
  }

  isDead() {
    return this.alpha <= 0;
  }
}

class Rocket {
  constructor(x, targetY) {
    this.x = x;
    this.y = canvas.height;
    this.targetY = targetY;
    this.vy = -8;
    this.exploded = false;
    this.color = 'white';
    this.palette = colorPalettes[Math.floor(Math.random() * colorPalettes.length)];
  }

  update() {
    this.y = this.y + this.vy;

    if (this.y <= this.targetY) {
      this.explode();
    }
  }

  draw() {
    ctx.beginPath();
    ctx.arc(this.x, this.y, 2, 0, Math.PI * 2);
    ctx.fillStyle = this.color;
    ctx.fill();

    ctx.beginPath();
    ctx.moveTo(this.x, this.y);
    ctx.lineTo(this.x, this.y + 10);
    ctx.strokeStyle = 'rgba(255, 255, 255, 0.5)';
    ctx.lineWidth = 1;
    ctx.stroke();
  }

  explode() {
    this.exploded = true;

    const particleCount = 100 + Math.random() * 50;

    for (let i = 0; i < particleCount; i++) {
      const color = this.palette[Math.floor(Math.random() * this.palette.length)];
      particles.push(new Particle(this.x, this.y, color));
    }
  }
}

function launchRocket(x, targetY) {
  rockets.push(new Rocket(x, targetY));
}

canvas.addEventListener('click', function(event) {
  const rect = canvas.getBoundingClientRect();
  const x = event.clientX - rect.left;
  const targetY = event.clientY - rect.top;
  launchRocket(x, targetY);
});

const toggleBtn = document.getElementById('toggleAuto');
const autoStatus = document.getElementById('autoStatus');

toggleBtn.addEventListener('click', function() {
  autoLaunch = !autoLaunch;
  autoStatus.textContent = autoLaunch ? 'ON' : 'OFF';
});

let autoLaunchTimer = 0;

function randomLaunch() {
  if (autoLaunch) {
    autoLaunchTimer = autoLaunchTimer + 1;

    if (autoLaunchTimer > 60) {
      const x = Math.random() * canvas.width;
      const targetY = Math.random() * canvas.height * 0.5 + 50;
      launchRocket(x, targetY);
      autoLaunchTimer = 0;
    }
  }
}

function animate() {
  ctx.fillStyle = 'rgba(0, 4, 40, 0.15)';
  ctx.fillRect(0, 0, canvas.width, canvas.height);

  for (let i = rockets.length - 1; i >= 0; i--) {
    rockets[i].update();
    rockets[i].draw();

    if (rockets[i].exploded) {
      rockets.splice(i, 1);
    }
  }

  for (let i = particles.length - 1; i >= 0; i--) {
    particles[i].update();
    particles[i].draw();

    if (particles[i].isDead()) {
      particles.splice(i, 1);
    }
  }

  randomLaunch();
  requestAnimationFrame(animate);
}

animate();
```

クリック打ち上げと自動打ち上げの両方が動くことを確認してください。

### コードを読み解こう

#### クリック打ち上げと自動打ち上げを同じ関数にまとめる

```js
function launchRocket(x, targetY) {
  rockets.push(new Rocket(x, targetY));
}
```

クリックでも自動でも、結局やりたいことは「ロケットを1発追加する」です。同じ処理は関数にまとめると扱いやすくなります。

#### `autoLaunch` ：モードのON/OFF

```js
let autoLaunch = false;
```

今、自動打ち上げモードかどうかを真偽値で持っています。

#### `autoLaunchTimer` ：何フレーム待ったかを数える

```js
let autoLaunchTimer = 0;
```

毎フレーム1ずつ増やして、一定値を超えたら1発打ち上げています。

#### ここまでの技術が全部入っている

この完成版には、

1. Canvasの準備
2. クリック座標
3. アニメーションループ
4. ロケット配列
5. 爆発粒子
6. 重力とフェード
7. 色パレット
8. 残像と発光
9. クラス設計
10. DOM操作と自動モード

が全部入っています。

### 改造しよう

#### ミッション①：自動打ち上げの頻度を変えよう

```js
if (autoLaunchTimer > 60) {
```

小さくすると連発し、大きくするとゆっくりになります。

#### ミッション②：色パレットを増やそう

新しい花火色を追加すると、見た目が一気に変わります。

#### ミッション③：UIの文言や色を変えよう

花火大会のテーマに合わせて、見た目も自分らしく調整してみましょう。


#### チェックしてみよう

✅️ クリックで花火を打ち上げられる
✅️ 自動打ち上げのON/OFFを切り替えられる
✅️ ロケットと粒をクラスで管理している
✅️ 軌跡と発光の表現が入っている
✅️ いまの `hanabi` アプリに近い構成で動いている

