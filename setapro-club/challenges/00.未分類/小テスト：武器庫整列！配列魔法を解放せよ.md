## 「小テスト：武器庫整列！配列魔法を解放せよ」

## ミッション：配列から取り出す、追加する、数えるを短いコードで確認しよう！

この小テストでは、本編で学んだ配列、`push()`、`length`、添字を使います。

### 解くときの約束

- 配列の中身を目で追いながら考えてください
- 値を1個追加したあと、何個入っているかも確認してください


### 問1：ウォームアップ

次のコードの `TODO` を埋めてください。

```js
const items = ['炎のつえ', '氷のゆびわ', '回復の薬'];
const text = document.querySelector('#text');

// TODO
```

#### 条件

- 変更は `1行`
- `回復の薬` を表示する

#### ヒント

配列は `0` 番目から始まります。

#### 確認ポイント

✅️ 3つ目の要素を取り出せる
✅️ 添字を使えている


### 問2：ミニ実装

次のコードに、取得したアイテムを配列へ追加する処理を入れてください。

```html
<button id="btn">入手</button>
<p id="count">0個</p>
```

```js
const btn = document.querySelector('#btn');
const count = document.querySelector('#count');

const inventory = [];
const item = '炎のつえ';

btn.addEventListener('click', function() {
  // TODO
  count.textContent = inventory.length + '個';
});
```

#### 条件

- 変更は `1行`
- `push()` を使う

#### 確認ポイント

✅️ 押すたびに個数が増える
✅️ 配列に追加できている


### 問3：できたら挑戦

次のコードに、ランダムで1個選ぶ処理を追加してください。

```js
const lootTable = ['炎のつえ', '氷のゆびわ', '回復の薬'];
const text = document.querySelector('#text');

const randomIndex = Math.floor(Math.random() * lootTable.length);

// TODO
```

#### お題

`randomIndex` を使って、選ばれたアイテム名を `text.textContent` に表示してください。

#### 条件

- 変更は `1行`
- `lootTable[randomIndex]` の形を使う

#### 確認ポイント

✅️ ランダムに1個表示される
✅️ `length` を使ったランダム選択を理解できている

